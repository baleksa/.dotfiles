# Function to source files if they exist
function safe_source() {
    [ -f "$1" ] && source "$1"
}

# Search words with sdcv
function d()
{
   sdcv "$1" | nvim +Man!
}

function dmalloc { eval `command dmalloc -b $*`; }
